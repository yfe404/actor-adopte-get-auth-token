{
    "actorSpecification": 1,
    "fields": {
        "apiRefreshToken": "string",
        "authToken": "string",
        "authTokensStatus": "number",
        "boostBody": "string",
        "boostStatus": "date",
        "success": "boolean"
    },
    "views": {
        "overview": {
            "title": "Overview",
            "transformation": {
                "fields": [
                    "apiRefreshToken",
                    "authToken",
                    "authtokensStatus",
                    "boostBody",
                    "boostStatus",
                    "success"
                ]
            },
            "display": {
                "component": "table",
                "properties": {
                    "apiRefreshToken": { "label": "JWT refresh token", "format": "text" },
                    "authToken": { "label": "JWT authentication token", "format": "text" },
                    "authTokenStatus": { "label": "https code auth token response", "format": "number" },
                    "boostBody": { "label": "GET /boost response body", "format": "object" },
                    "boostStatus": { "label": "GET /boost response status", "format": "number" },
                    "success": { "label": "Run status", "format": "boolean" }
                }
            }
        }
    }
}
